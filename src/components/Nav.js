import { useState } from 'react'
import styled from 'styled-components'

const StyledWrapper = styled.div`
    width: 100%;
    padding: 2em;
`

const Nav = () => {
    return (
        <StyledWrapper>
            Nav
        </StyledWrapper>
    )
}

export default Nav;
